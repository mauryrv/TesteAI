# Copie para .env na raiz (e ajuste apps/web/.env.local se necessário)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/controle_gado
JWT_SECRET=change-me-in-development-only
PORT=4000
CORS_ORIGIN=http://localhost:3000

# Frontend (Next.js) — também em apps/web/.env.local
NEXT_PUBLIC_API_URL=http://localhost:4000
