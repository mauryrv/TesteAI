/**
 * Application layer: use cases orchestrating domain + ports (repositories).
 * Stubs only — implement handlers as features land.
 */
export const APPLICATION_LAYER = "controle-gado-application";
